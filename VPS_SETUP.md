# 🖥️ Установка бота на свой сервер (VPS)

Полная инструкция по установке Telegram-бота на собственный VPS сервер.

---

## 📋 Где купить сервер (дёшево и надёжно)

### 🇷🇺 Российские провайдеры:

1. **Timeweb** - от 150₽/мес
   - 👉 https://timeweb.com/ru/services/vps/
   - ✅ Русский интерфейс
   - ✅ Поддержка 24/7
   - ✅ Много дата-центров

2. **Reg.ru** - от 200₽/мес
   - 👉 https://www.reg.ru/vps/
   - ✅ Надёжный провайдер
   - ✅ Быстрая поддержка

3. **Beget** - от 150₽/мес
   - 👉 https://beget.com/ru/vps
   - ✅ Хорошая репутация

### 🌍 Международные (дешевле):

1. **Contabo** - от €4/мес (~400₽)
   - 👉 https://contabo.com
   - ✅ Очень мощные серверы за свои деньги
   - ✅ 200GB SSD, 8GB RAM

2. **Hetzner** - от €4.5/мес (~450₽)
   - 👉 https://www.hetzner.com/cloud
   - ✅ Один из лучших по соотношению цена/качество
   - ✅ Германия

3. **DigitalOcean** - от $6/мес (~600₽)
   - 👉 https://www.digitalocean.com
   - ✅ Очень популярный
   - ✅ Простой интерфейс

4. **Vultr** - от $6/мес (~600₽)
   - 👉 https://www.vultr.com
   - ✅ Много локаций

### 💰 Бесплатные варианты (с ограничениями):

1. **Oracle Cloud** - Always Free
   - 👉 https://www.oracle.com/cloud/free/
   - ✅ Навсегда бесплатно
   - ✅ 2 VM инстанса
   - ⚠️ Нужна карта (не списывается)

2. **Google Cloud** - $300 на 90 дней
   - 👉 https://cloud.google.com/free
   - ✅ Большой кредит на старт
   - ⚠️ После окончания - платно

---

## 🚀 Установка на Ubuntu/Debian (самое популярное)

### Шаг 1: Подключение к серверу

После покупки VPS вы получите:
- IP адрес (например: 192.168.1.100)
- Root пароль
- Или SSH ключ

**Подключение через SSH:**

```bash
# MacOS/Linux:
ssh root@ВАШ_IP

# Windows (PowerShell):
ssh root@ВАШ_IP

# Или используйте PuTTY для Windows
```

---

### Шаг 2: Обновление системы

```bash
# Обновить список пакетов
sudo apt update

# Обновить все пакеты
sudo apt upgrade -y
```

---

### Шаг 3: Установка Python 3.11

```bash
# Установить необходимые пакеты
sudo apt install -y software-properties-common

# Добавить репозиторий deadsnakes (для новых версий Python)
sudo add-apt-repository ppa:deadsnakes/ppa -y

# Обновить список пакетов
sudo apt update

# Установить Python 3.11 и дополнительные инструменты
sudo apt install -y python3.11 python3.11-venv python3.11-pip git

# Проверить версию
python3.11 --version
# Должно показать: Python 3.11.x
```

---

### Шаг 4: Создание пользователя для бота (безопасность)

```bash
# Создать пользователя
sudo adduser botuser

# Добавить в группу sudo (опционально)
sudo usermod -aG sudo botuser

# Переключиться на нового пользователя
su - botuser
```

---

### Шаг 5: Клонирование репозитория

```bash
# Клонировать ваш репозиторий
git clone https://github.com/m1cch/ausn-telegram-bot.git

# Перейти в папку
cd ausn-telegram-bot

# Проверить файлы
ls -la
```

---

### Шаг 6: Создание виртуального окружения

```bash
# Создать виртуальное окружение с Python 3.11
python3.11 -m venv venv

# Активировать окружение
source venv/bin/activate

# Обновить pip
pip install --upgrade pip
```

---

### Шаг 7: Установка зависимостей

```bash
# Установить все зависимости из requirements.txt
pip install -r requirements.txt

# Проверить установку
pip list
```

---

### Шаг 8: Создание .env файла

```bash
# Создать .env файл
nano .env

# Добавить токен (вставьте и нажмите Ctrl+X, затем Y, затем Enter):
TELEGRAM_BOT_TOKEN=8582141259:AAGYjXmur00TkzS9WcXFpq-xnlPPVmdhe7g

# Или одной командой:
echo "TELEGRAM_BOT_TOKEN=8582141259:AAGYjXmur00TkzS9WcXFpq-xnlPPVmdhe7g" > .env
```

---

### Шаг 9: Тестовый запуск

```bash
# Запустить бота для проверки
python bot.py

# Вы должны увидеть:
# INFO - Бот запущен...
# INFO - Application started

# Остановить: Ctrl+C
```

---

## 🔧 Настройка автозапуска (systemd service)

Чтобы бот работал всегда, даже после перезагрузки сервера:

### Шаг 1: Создать systemd service файл

```bash
# Выйти из виртуального окружения (если внутри)
deactivate

# Создать service файл (от root или с sudo)
sudo nano /etc/systemd/system/ausn-bot.service
```

### Шаг 2: Вставить конфигурацию

```ini
[Unit]
Description=AUSN Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/ausn-telegram-bot
Environment="PATH=/home/botuser/ausn-telegram-bot/venv/bin"
ExecStart=/home/botuser/ausn-telegram-bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Сохранить:** Ctrl+X → Y → Enter

### Шаг 3: Запустить сервис

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable ausn-bot

# Запустить бота
sudo systemctl start ausn-bot

# Проверить статус
sudo systemctl status ausn-bot

# Должно показать: active (running) 🟢
```

### Шаг 4: Полезные команды

```bash
# Просмотр логов в реальном времени
sudo journalctl -u ausn-bot -f

# Последние 50 строк логов
sudo journalctl -u ausn-bot -n 50

# Перезапустить бота
sudo systemctl restart ausn-bot

# Остановить бота
sudo systemctl stop ausn-bot

# Статус бота
sudo systemctl status ausn-bot
```

---

## 🔒 Настройка безопасности

### 1. Настройка файрвола (UFW)

```bash
# Установить UFW
sudo apt install -y ufw

# Разрешить SSH (ВАЖНО! Иначе потеряете доступ)
sudo ufw allow ssh
sudo ufw allow 22

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

### 2. Отключить root логин по SSH

```bash
# Открыть конфиг SSH
sudo nano /etc/ssh/sshd_config

# Найти и изменить:
PermitRootLogin no
PasswordAuthentication no  # Если используете SSH ключи

# Перезапустить SSH
sudo systemctl restart sshd
```

### 3. Установить Fail2Ban (защита от брутфорса)

```bash
# Установить
sudo apt install -y fail2ban

# Запустить и включить автозапуск
sudo systemctl start fail2ban
sudo systemctl enable fail2ban

# Проверить статус
sudo systemctl status fail2ban
```

---

## 🔄 Обновление бота

Когда вы обновите код на GitHub:

```bash
# Перейти в папку проекта
cd /home/botuser/ausn-telegram-bot

# Получить изменения
git pull

# Активировать окружение
source venv/bin/activate

# Обновить зависимости (если изменились)
pip install -r requirements.txt

# Перезапустить бота
sudo systemctl restart ausn-bot

# Проверить логи
sudo journalctl -u ausn-bot -f
```

---

## 📊 Мониторинг бота

### 1. Просмотр логов

```bash
# Логи в реальном времени
sudo journalctl -u ausn-bot -f

# Последние 100 строк
sudo journalctl -u ausn-bot -n 100

# Логи за сегодня
sudo journalctl -u ausn-bot --since today

# Поиск ошибок
sudo journalctl -u ausn-bot | grep ERROR
```

### 2. Мониторинг ресурсов

```bash
# Общая информация
htop

# Процессы Python
ps aux | grep python

# Использование памяти
free -h

# Место на диске
df -h
```

---

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверить логи
sudo journalctl -u ausn-bot -n 50

# Проверить синтаксис Python
cd /home/botuser/ausn-telegram-bot
source venv/bin/activate
python bot.py
```

### Ошибка с правами доступа

```bash
# Изменить владельца файлов
sudo chown -R botuser:botuser /home/botuser/ausn-telegram-bot

# Права на .env
chmod 600 /home/botuser/ausn-telegram-bot/.env
```

### Бот не отвечает в Telegram

```bash
# Проверить, что бот запущен
sudo systemctl status ausn-bot

# Проверить сеть
ping telegram.org

# Проверить токен
cat /home/botuser/ausn-telegram-bot/.env
```

### Нет интернета на сервере

```bash
# Проверить DNS
cat /etc/resolv.conf

# Добавить Google DNS
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf

# Проверить подключение
ping 8.8.8.8
```

---

## 🎯 Быстрая установка (скрипт)

Создайте файл `install.sh`:

```bash
#!/bin/bash
echo "🚀 Установка AUSN Telegram Bot..."

# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python 3.11
sudo add-apt-repository ppa:deadsnakes/ppa -y
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3.11-pip git

# Клонирование репозитория
git clone https://github.com/m1cch/ausn-telegram-bot.git
cd ausn-telegram-bot

# Создание окружения
python3.11 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

# Создание .env
read -p "Введите токен бота: " TOKEN
echo "TELEGRAM_BOT_TOKEN=$TOKEN" > .env

# Тестовый запуск
echo "✅ Установка завершена! Запускаю бота для проверки..."
python bot.py
```

Запуск:
```bash
chmod +x install.sh
./install.sh
```

---

## 📈 Дополнительные улучшения

### 1. Автоматическое обновление с GitHub

Создать cron задачу:
```bash
# Открыть crontab
crontab -e

# Добавить (проверка обновлений каждые 5 минут):
*/5 * * * * cd /home/botuser/ausn-telegram-bot && git pull && sudo systemctl restart ausn-bot
```

### 2. Бэкап логов

```bash
# Создать папку для бэкапов
mkdir -p /home/botuser/backups

# Бэкап логов раз в день
0 0 * * * sudo journalctl -u ausn-bot > /home/botuser/backups/bot-$(date +\%Y\%m\%d).log
```

### 3. Уведомления о перезапуске

Добавьте в код бота отправку сообщения администратору при старте.

---

## 💰 Стоимость хостинга

| Провайдер | Цена/мес | RAM | CPU | Диск | Трафик |
|-----------|----------|-----|-----|------|--------|
| Timeweb | 150₽ | 1GB | 1 | 10GB | безлимит |
| Contabo | €4 (~400₽) | 4GB | 2 | 50GB | безлимит |
| Hetzner | €4.5 (~450₽) | 4GB | 2 | 40GB | 20TB |
| DigitalOcean | $6 (~600₽) | 1GB | 1 | 25GB | 1TB |

**Для бота достаточно минимального тарифа!**

---

## ✅ Чеклист после установки

- [ ] Бот запущен и отвечает в Telegram
- [ ] Настроен systemd service (автозапуск)
- [ ] Включен файрвол (UFW)
- [ ] Установлен Fail2Ban
- [ ] Настроен мониторинг логов
- [ ] Проверена работа после перезагрузки сервера

---

## 🎉 Готово!

Ваш бот теперь работает на собственном сервере 24/7!

**Проверка работы:**
```bash
# Статус
sudo systemctl status ausn-bot

# Логи
sudo journalctl -u ausn-bot -f

# Открыть бота в Telegram
# Отправить /start
```

---

## 📚 Полезные ссылки

- **Документация systemd:** https://systemd.io/
- **UFW Guide:** https://help.ubuntu.com/community/UFW
- **Python venv:** https://docs.python.org/3/library/venv.html

---

**Нужна помощь?** Пишите, помогу на любом этапе! 🚀

