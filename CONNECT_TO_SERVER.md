# 🔐 Как подключиться к серверу

Пошаговая инструкция для подключения к VPS/VDS после покупки.

---

## 📧 Шаг 1: Найти данные для подключения

После покупки сервера вам должны были прислать (проверьте):
- 📧 **Email** от хостинг-провайдера
- 🌐 **Личный кабинет** на сайте

**Вам нужны:**
```
IP адрес: 123.45.67.89 (например)
Логин: root (обычно)
Пароль: Ваш_пароль123
```

**Где найти в популярных хостингах:**

### Timeweb:
1. Зайти на timeweb.com
2. Личный кабинет → VPS/VDS
3. Нажать на ваш сервер
4. Там будет IP и кнопка "Показать пароль"

### Reg.ru:
1. Личный кабинет → Услуги → VPS
2. Выбрать сервер
3. Данные для доступа

### Contabo:
1. Письмо "Your VPS is ready"
2. Или Customer Control Panel → VPS Control

---

## 🖥️ Шаг 2: Выбрать способ подключения

### Для macOS (у вас):

**Способ 1: Через встроенный Terminal (РЕКОМЕНДУЮ)**

1. Откройте **Terminal** (Терминал)
   - Нажмите `Cmd + Space`
   - Введите "Terminal"
   - Нажмите Enter

2. Введите команду:
```bash
ssh root@ВАШ_IP
```

**Пример:**
```bash
ssh root@123.45.67.89
```

3. При первом подключении спросят:
```
Are you sure you want to continue connecting (yes/no)?
```
Введите: `yes` и нажмите Enter

4. Введите пароль (при вводе символы не отображаются - это нормально!)

5. Если всё правильно - вы подключены! ✅

---

**Способ 2: Через iTerm2 (если установлен)**

То же самое, просто в iTerm2 вместо Terminal.

---

### Для Windows:

**Способ 1: PowerShell (Windows 10/11)**

1. Нажмите `Win + X`
2. Выберите "Windows PowerShell"
3. Введите:
```bash
ssh root@ВАШ_IP
```

**Способ 2: PuTTY (классика)**

1. Скачать PuTTY: https://www.putty.org/
2. Запустить PuTTY
3. В поле "Host Name": ваш IP (123.45.67.89)
4. Port: 22
5. Connection type: SSH
6. Нажать "Open"
7. Ввести логин: root
8. Ввести пароль

---

## ✅ Вы подключились, если видите:

```bash
Welcome to Ubuntu 22.04.3 LTS

root@server:~#
```

Или что-то похожее с приглашением командной строки.

---

## 🚀 Шаг 3: Первые команды на сервере

После подключения выполните для проверки:

```bash
# Проверить систему
uname -a

# Проверить версию ОС
cat /etc/os-release

# Проверить доступное место
df -h

# Проверить память
free -h

# Проверить подключение к интернету
ping -c 3 google.com
```

Если всё работает - отлично! Можно устанавливать бота!

---

## 📝 Полная установка бота (копируй и вставляй):

### Шаг 1: Обновить систему

```bash
sudo apt update && sudo apt upgrade -y
```

Это займёт 2-5 минут. Дождитесь завершения.

---

### Шаг 2: Установить Python 3.11 и Git

```bash
# Добавить репозиторий с новыми версиями Python
sudo add-apt-repository ppa:deadsnakes/ppa -y

# Обновить список пакетов
sudo apt update

# Установить Python 3.11, pip, venv и git
sudo apt install -y python3.11 python3.11-venv python3.11-pip git

# Проверить версию
python3.11 --version
```

Должно показать: `Python 3.11.x`

---

### Шаг 3: Клонировать репозиторий

```bash
# Клонировать ваш бот
git clone https://github.com/m1cch/ausn-telegram-bot.git

# Перейти в папку
cd ausn-telegram-bot

# Проверить, что файлы на месте
ls -la
```

Должны увидеть: `bot.py`, `requirements.txt` и другие файлы.

---

### Шаг 4: Создать виртуальное окружение

```bash
# Создать окружение
python3.11 -m venv venv

# Активировать окружение
source venv/bin/activate

# Должно появиться (venv) в начале строки:
# (venv) root@server:~/ausn-telegram-bot#
```

---

### Шаг 5: Установить зависимости

```bash
# Обновить pip
pip install --upgrade pip

# Установить зависимости бота
pip install -r requirements.txt

# Это займёт 1-2 минуты
```

---

### Шаг 6: Создать .env файл с токеном

```bash
# Создать файл .env
nano .env
```

Откроется текстовый редактор. Вставьте:
```
TELEGRAM_BOT_TOKEN=8582141259:AAGYjXmur00TkzS9WcXFpq-xnlPPVmdhe7g
```

**Как сохранить:**
1. Нажмите `Ctrl + X`
2. Нажмите `Y` (Yes)
3. Нажмите `Enter`

**Или одной командой:**
```bash
echo "TELEGRAM_BOT_TOKEN=8582141259:AAGYjXmur00TkzS9WcXFpq-xnlPPVmdhe7g" > .env
```

---

### Шаг 7: Тестовый запуск!

```bash
python bot.py
```

Вы должны увидеть:
```
INFO - Бот запущен...
INFO - Application started
```

✅ **Бот работает!**

**Проверка:** Откройте бота в Telegram и отправьте `/start`

**Остановить бота:** Нажмите `Ctrl + C`

---

### Шаг 8: Настроить автозапуск (чтобы работал 24/7)

```bash
# Создать systemd service файл
sudo nano /etc/systemd/system/ausn-bot.service
```

Вставьте (замените пути, если нужно):
```ini
[Unit]
Description=AUSN Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/ausn-telegram-bot
Environment="PATH=/root/ausn-telegram-bot/venv/bin"
ExecStart=/root/ausn-telegram-bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохранить: `Ctrl + X` → `Y` → `Enter`

**Запустить сервис:**
```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable ausn-bot

# Запустить бота
sudo systemctl start ausn-bot

# Проверить статус
sudo systemctl status ausn-bot
```

Должно показать: **active (running)** 🟢

---

### Шаг 9: Проверить логи

```bash
# Смотреть логи в реальном времени
sudo journalctl -u ausn-bot -f

# Последние 50 строк
sudo journalctl -u ausn-bot -n 50
```

Для выхода из просмотра логов: `Ctrl + C`

---

## 🎉 Готово! Бот работает 24/7!

Теперь бот будет:
- ✅ Работать постоянно
- ✅ Автоматически запускаться после перезагрузки сервера
- ✅ Перезапускаться при ошибках

---

## 🔧 Полезные команды для управления ботом:

```bash
# Перезапустить бота
sudo systemctl restart ausn-bot

# Остановить бота
sudo systemctl stop ausn-bot

# Запустить бота
sudo systemctl start ausn-bot

# Статус бота
sudo systemctl status ausn-bot

# Логи бота
sudo journalctl -u ausn-bot -f
```

---

## 🔄 Обновление бота (после изменений в коде):

```bash
# Перейти в папку
cd /root/ausn-telegram-bot

# Получить изменения из GitHub
git pull

# Активировать окружение
source venv/bin/activate

# Обновить зависимости (если изменились)
pip install -r requirements.txt

# Перезапустить бота
sudo systemctl restart ausn-bot

# Проверить логи
sudo journalctl -u ausn-bot -f
```

---

## ❓ Решение проблем:

### Не могу подключиться по SSH

**Ошибка: "Connection refused"**
```bash
# Проверьте IP адрес
# Проверьте, что сервер запущен в личном кабинете хостинга
```

**Ошибка: "Permission denied"**
```bash
# Проверьте пароль (возможно, нужно сбросить в личном кабинете)
# Убедитесь, что используете правильный логин (root или ubuntu)
```

### Ошибка при установке Python

```bash
# Если нет add-apt-repository:
sudo apt install software-properties-common -y

# Затем повторите установку
sudo add-apt-repository ppa:deadsnakes/ppa -y
```

### Бот не запускается

```bash
# Проверить логи
sudo journalctl -u ausn-bot -n 100

# Проверить, что .env файл создан
cat /root/ausn-telegram-bot/.env

# Проверить права доступа
ls -la /root/ausn-telegram-bot/
```

---

## 🔒 Безопасность (опционально, но рекомендуется):

### Настроить файрвол:

```bash
# Установить UFW
sudo apt install -y ufw

# Разрешить SSH (ВАЖНО!)
sudo ufw allow ssh
sudo ufw allow 22

# Включить файрвол
sudo ufw enable

# Проверить
sudo ufw status
```

### Установить Fail2Ban (защита от брутфорса):

```bash
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

---

## 📊 Мониторинг сервера:

```bash
# Посмотреть использование ресурсов
htop

# Если htop не установлен:
sudo apt install htop -y
htop

# Использование диска
df -h

# Использование памяти
free -h

# Процессы Python
ps aux | grep python
```

---

## 💡 Советы:

1. **Сохраните данные доступа** (IP, логин, пароль) в надёжное место
2. **Сделайте скриншот** успешного подключения
3. **Проверяйте логи** время от времени: `sudo journalctl -u ausn-bot -f`
4. **Обновляйте систему** раз в неделю: `sudo apt update && sudo apt upgrade -y`

---

## 🆘 Нужна помощь?

Если что-то не получается:
1. Скопируйте текст ошибки
2. Скопируйте вывод команды `sudo journalctl -u ausn-bot -n 50`
3. Напишите мне - помогу разобраться!

---

## ✅ Чеклист установки:

- [ ] Подключился к серверу по SSH
- [ ] Обновил систему (`apt update && upgrade`)
- [ ] Установил Python 3.11
- [ ] Клонировал репозиторий
- [ ] Создал виртуальное окружение
- [ ] Установил зависимости
- [ ] Создал .env файл
- [ ] Протестировал запуск (`python bot.py`)
- [ ] Настроил systemd service
- [ ] Включил автозапуск
- [ ] Проверил статус (`systemctl status ausn-bot`)
- [ ] Протестировал бота в Telegram

---

**Бот работает 24/7!** 🎉

**GitHub репозиторий:** https://github.com/m1cch/ausn-telegram-bot

