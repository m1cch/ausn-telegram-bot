# 🔧 Инструкция по настройке бота

## Шаг 1: Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя вашего бота (например: "Калькулятор АУСН")
4. Введите username бота (должен заканчиваться на `bot`, например: `ausn_calc_bot`)
5. BotFather пришлёт вам токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта и добавьте в него:

```bash
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

Например:
```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 3: Установка зависимостей

```bash
# Рекомендуется создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
# На macOS/Linux:
source venv/bin/activate
# На Windows:
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 4: Запуск бота

```bash
python bot.py
```

Если всё настроено правильно, вы увидите сообщение:
```
INFO - Бот запущен...
```

## Шаг 5: Тестирование

1. Найдите своего бота в Telegram по username
2. Нажмите "Start" или отправьте `/start`
3. Следуйте инструкциям бота

## 🐛 Возможные проблемы

### Ошибка: "TELEGRAM_BOT_TOKEN не найден"
- Убедитесь, что файл `.env` находится в той же папке, что и `bot.py`
- Проверьте, что в файле нет лишних пробелов или кавычек
- Формат должен быть: `TELEGRAM_BOT_TOKEN=ваш_токен` (без пробелов вокруг `=`)

### Ошибка: "Invalid token"
- Проверьте, что вы скопировали токен полностью
- Токен должен содержать двоеточие и не должен содержать пробелов
- Получите новый токен у BotFather командой `/token`

### Бот не отвечает
- Убедитесь, что бот запущен (скрипт должен быть запущен)
- Проверьте интернет-соединение
- Попробуйте отправить `/start` повторно

## 💡 Полезные команды BotFather

- `/mybots` — список вашихботов
- `/token` — получить токен бота заново
- `/setdescription` — установить описание бота
- `/setabouttext` — установить текст "О боте"
- `/setuserpic` — установить аватар бота

## 🚀 Запуск в production

Для постоянной работы бота на сервере используйте:

### Systemd (Linux)
```bash
sudo nano /etc/systemd/system/ausn-bot.service
```

Добавьте:
```ini
[Unit]
Description=AUSN Calculator Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable ausn-bot
sudo systemctl start ausn-bot
```

### Screen (простой вариант)
```bash
screen -S ausn-bot
python bot.py
# Нажмите Ctrl+A, затем D для отключения
```

### PM2 (для Node.js, но работает и с Python)
```bash
npm install -g pm2
pm2 start bot.py --interpreter python3 --name ausn-bot
pm2 save
pm2 startup
```

---

Если у вас возникли проблемы, проверьте логи — бот выводит подробную информацию об ошибках.

